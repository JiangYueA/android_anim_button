<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- AnimImageView -->
    <declare-styleable name="AnimImageView">
        <attr name="firstDraw" format="reference" />
        <attr name="secondDraw" format="reference" />
    </declare-styleable>

    <!-- FillPopImageView -->
    <declare-styleable name="FillPopImageView">
        <attr name="firstView" format="reference" />
        <attr name="secondView" format="reference" />
    </declare-styleable>

    <!-- RippleImageView -->
    <declare-styleable name="RippleImageView">
        <attr name="rb_color" format="color" />
        <attr name="rb_strokeWidth" format="dimension" />
        <attr name="rb_radius" format="dimension" />
        <attr name="rb_duration" format="integer" />
        <attr name="rb_rippleAmount" format="integer" />
        <attr name="rb_scale" format="float" />
        <attr name="rb_type" format="enum">
            <enum name="fillRipple" value="0" />
            <enum name="strokeRipple" value="1" />
        </attr>
    </declare-styleable>

    <!-- 基金组合滑块 2016-6-16 16:12:00 jiang -->
    <declare-styleable name="GalleryRecyclerView">
        <attr name="rvp_triggerOffset" format="float" />
        <attr name="rvp_flingFactor" format="float" />
        <attr name="rvp_singlePageFling" format="boolean" />
    </declare-styleable>

</resources>
